<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/dialog_bg">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Game Time"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginBottom="20dp"/>

    <!-- GRID -->
    <GridLayout
        android:id="@+id/gridTimers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="2"
        android:rowCount="2"
        android:useDefaultMargins="true">

        <include layout="@layout/item_time_card" android:id="@+id/card1min"/>
        <include layout="@layout/item_time_card" android:id="@+id/card3min"/>
        <include layout="@layout/item_time_card" android:id="@+id/card5min"/>
        <include layout="@layout/item_time_card" android:id="@+id/card10min"/>

    </GridLayout>

    <!-- NO TIMER (FULL WIDTH) -->
    <include
        android:id="@+id/cardNoTimer"
        layout="@layout/item_time_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"/>

    <!-- CANCEL -->
    <TextView
        android:id="@+id/btnCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textColor="#888888"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginTop="20dp"/>

</LinearLayout>